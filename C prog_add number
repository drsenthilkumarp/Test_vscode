#include <stdio.h>
int main() {    

    int number1, number2, sum;
    
    number1=4;
    number2=6;

    // calculate the sum
    sum = number1 + number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
} 
